// var for where to put the output
var desktopResults = document.getElementById("desktopSection");


// Testing new way of going through each DE framework section and then displaying EVERYTHING (for now)
function newLoopTest(data) {

    // data = the complete JSON

    // Outer loop 
    $.each(data, function(key) {
        // The key is 'gtk2' or 'qt'

        var currentFramework = key;


        var StrFW = JSON.stringify(currentFramework);
        var StrThis = JSON.stringify(this);

        // Should output the fameworks 
        console.log(StrFW);

        document.write("Current Framework: <strong>" + currentFramework +  "</strong><br />" + StrThis + "<br /><hr />" );


        /* 
        // ATM the vars below in the temp literal and copy and pasted ... aka NOT CORRECT 
        const fwOutput = `
            
        Desktop Name: <bold>${currentDEName}</bold>
        
        <br />
        Homepage:<a href="${currentDEHomepage}" target="_blank"> ${currentDEName}'s Homepage 
        </a>
        
        <br />
        Github URL: <a href="${currentDEGithub}" target="_blank"> ${currentDEName} on Github 
        
        </a>
        <br />
        <hr />
        `;

        document.write(fwOutput);
        */

    });
}

// "main" function
function getDesktopData(desktopData) {
    jsonURL = "http://xmetal.x10.mx/linuxHQ/json/desktops.json";

    // Get JSON Data
    $.getJSON(jsonURL, function(data) {


        newLoopTest(data);

    }); // ends getJSON loop
}